# Website Builder - Environment Variables Example

# ============================================
# REQUIRED SECRETS (Set in Bolt/Supabase)
# ============================================

# Runtime mode: "development" | "staging" | "production"
VITE_RUNTIME_MODE=production

# Supabase (auto-populated by Supabase)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# SECURITY / CSP
# ============================================

# CSP reporting (true = report-only, false = enforce)
CSP_REPORT_ONLY=false

# Hub / Event Bus (set to "disabled" to skip)
HUB_URL=https://events.craudiovizai.com
HUB_SIGNING_KEY=replace-with-32-plus-char-random-secret

# CAPTCHA (none | hcaptcha | recaptcha)
CAPTCHA_PROVIDER=none
HCAPTCHA_SECRET=your-hcaptcha-secret
RECAPTCHA_SECRET=your-recaptcha-secret

# Tracking (for signed open/click tokens)
TRACKING_DOMAIN=

# Deploy Providers
VERCEL_TOKEN=
NETLIFY_TOKEN=

# AI Providers (Javari-first, fallback to OpenAI/Anthropic)
JAVARI_API_URL=
JAVARI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Internal Bypass (for testing/development - NEVER true in production)
INTERNAL_BYPASS_MODE=false
INTERNAL_UNLIMITED_ORG_IDS=
INTERNAL_EMAIL_DOMAIN=craudiovizai.com

# ============================================
# BOLT CONFIGURATION
# ============================================

# For Bolt deployment, set these three secrets:
# 1. VITE_RUNTIME_MODE=cloud
# 2. HUB_URL=disabled
# 3. HUB_SIGNING_KEY=disabled

# CAPTCHA is optional, but recommended for production:
# 4. CAPTCHA_PROVIDER=hcaptcha (or recaptcha)
# 5. HCAPTCHA_SECRET=your-secret (or RECAPTCHA_SECRET)

# All other secrets are optional and can be added later.
